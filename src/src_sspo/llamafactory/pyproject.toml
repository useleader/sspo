[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "llamafactory"
version = "0.0.1"
description = "LLaMA-Factory SSPO fork"
requires-python = ">=3.10"
